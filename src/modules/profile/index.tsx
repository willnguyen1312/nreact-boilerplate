import { RouteComponentProps } from '@reach/router';
import React, { FunctionComponent } from 'react';

const Profile: FunctionComponent<RouteComponentProps> = props => {
  return <h1>Profile</h1>;
};

export default Profile;
