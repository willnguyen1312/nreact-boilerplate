export const logError = error => {
  // tslint:disable-next-line:no-console
  console.error(error);
};

export const logInfo = error => {
  // tslint:disable-next-line:no-console
  console.info(error);
};
