export const logError = (error: any) => {
  // tslint:disable-next-line:no-console
  console.error(error);
};

export const logInfo = (error: any) => {
  // tslint:disable-next-line:no-console
  console.info(error);
};
